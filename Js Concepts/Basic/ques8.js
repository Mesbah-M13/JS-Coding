// q8:  Given an array of strings, return a new array where all strings are in uppercase.

const myStrArray = ['hi','bye','khai','nai']
const newArr = myStrArray.map(arr=> arr.toUpperCase())
console.log(newArr);